# Environment files are split by app — do not use a root .env for secrets.

# Backend (API, PostgreSQL, JWT):
#   cp backend/.env.example backend/.env

# Frontend (Vite — browser-safe VITE_* vars only):
#   cp frontend/.env.example frontend/.env
